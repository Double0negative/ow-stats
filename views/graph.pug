html
  head
    script(src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.6.0/Chart.bundle.min.js")
    script(src="/scripts/colors.js")

    style.
      body {
        background: white;
      }

      .chart-container {
        width: 90%;
        margin: auto;
        margin-top: 30px;
      }

  body
    .title
      h1=id
    .chart-container
      canvas#chart
    script.
      var stats = !{JSON.stringify(stats)};
      var keySet = !{JSON.stringify(keys)};
      console.log(keySet)
    script(src="/scripts/line.js")
